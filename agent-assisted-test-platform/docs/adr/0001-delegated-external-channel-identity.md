# 外部渠道操作映射到真实平台用户

飞书等 IM 应用只承担消息入口和协议适配，不作为占用、释放等业务操作的共享操作者。适配器使用独立的集成客户端凭据证明渠道可信，但集成客户端不是业务角色；每个外部渠道身份必须显式绑定一个平台用户，平台始终按该用户的角色完成授权和审计，未绑定身份不得执行写操作。首次绑定由用户登录 Web 生成短时一次性绑定码，再在 IM 单聊中提交；系统不按姓名或手机号自动匹配，用户和 `ADMIN` 均可解除绑定。这样避免共享机器人账号或管理员 PAT 绕过权限，并确保跨 Web、API 和 IM 的操作历史指向同一自然人。
